---
- name: "Build VMware Packer Templates"
  hosts: localhost
  gather_facts: true
  connection: local

  collections:
    # - xyz.packer
    - xyz.vmware

  roles:
    # - xyz.packer.install
    - xyz.vmware.vm_templates